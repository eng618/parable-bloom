// Deprecated legacy LevelSolver stub.
// The canonical, coordinate-based solver lives in:
// `lib/providers/game_providers.dart` (LevelSolver).

class LevelSolver {
  LevelSolver._();

  static List<String>? solve(Object? _) {
    throw UnsupportedError(
      'Legacy LevelSolver removed. Use LevelSolver in lib/providers/game_providers.dart',
    );
  }
}
